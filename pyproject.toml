[project]
name = "canvas-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=2.14.3",
    "httpx>=0.28.1",
    "pypdf>=6.6.0",
    "python-dotenv>=1.2.1",
]

[project.scripts]
canvas-mcp = "src.server:main"
